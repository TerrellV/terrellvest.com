import React, { PropTypes } from 'react';
import { Link } from 'react-router';

const WebProjects = React.createClass({
  render () {
    return (
      <div>
        web projects
      </div>
    )
  }
})

export default WebProjects;
